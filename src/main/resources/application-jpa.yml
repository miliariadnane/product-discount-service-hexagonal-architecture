# application properties
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product-discount-service
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
